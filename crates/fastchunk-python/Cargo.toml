[package]
name = "fastchunk-python"
version = "0.1.0"
edition = "2021"

[lib]
name = "fastchunk"
crate-type = ["cdylib"]

[dependencies]
fastchunk-core = { path = "../fastchunk-core" }
pyo3 = { version = "0.21", features = ["extension-module"] }
