[package]
name = "fastchunk-core"
version = "0.1.0"
edition = "2021"
description = "Core Rust text chunking engine for FastChunk"
license = "Apache-2.0"
repository = "https://github.com/kushalkp88/fastchunk"

[dependencies]
regex = "1.10"
serde_json = "1.0"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "core_benchmark"
harness = false
